#include "allocator.hpp"
ArenaAllocator::ArenaAllocator(size_t size) { /* TODO */ }
ArenaAllocator::~ArenaAllocator() { /* TODO */ }
void *ArenaAllocator::allocate(size_t size, size_t alignment) { /* TODO */
  return nullptr;
}
void ArenaAllocator::reset() { /* TODO */ }
