#include "optimizer.hpp"
double newtonMethod(std::function<double(double)> f,
                    std::function<double(double)> df, double x0,
                    int maxIter) { /* TODO */
  return 0;
}
